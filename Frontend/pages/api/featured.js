export default function handler(req, res) {
    // Replace with actual featured products fetching logic
    const featuredPlans = [
        // Sample data
    ];
    res.status(200).json(featuredPlans);
}